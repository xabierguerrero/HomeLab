services:
  it-tools:
    image: corentinth/it-tools:latest
    container_name: it-tools
    hostname: it-tools_Evaru
    restart: unless-stopped
    enviroment:
      - TZ=Europe/Madrid
    networks:
      - tools_net
      
networks:
  tools_net:
    external: true
